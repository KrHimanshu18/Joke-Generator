<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>Laugh Out Loud</title>
  </head>

  <body>
    <div class="container">
      <h1>Let's find something for you ✌️</h1>

      <form action="/get-joke" id="form" class="form" method="POST">
        <select name="category" class="form-select">
          <option value="" data-display="Select">Category</option>
          <option value="Any">Any</option>
          <option value="Programming">Programming</option>
          <option value="Misc">Misc</option>
          <option value="Dark">Dark</option>
          <option value="Pun">Pun</option>
          <option value="Spooky">Spooky</option>
          <option value="Christmas">Christmas</option>
        </select>
        <select name="blackFlags" class="form-select">
          <option value="" data-display="Select">Black Flag</option>
          <option value="nsfw">nsfw</option>
          <option value="religious">religious</option>
          <option value="political">political</option>
          <option value="racist">racist</option>
          <option value="sexist">sexist</option>
          <option value="explicit">explicit</option>
        </select>
        <button type="submit" class="form-submit">Go</button>
      </form>
      <section id="cards" class="cards">
        <% if(locals.data){ %>
        <article class="card-item">
          <h2 class="card-activity">
            <!-- Show setup -->
            <%= data.setup %>
          </h2>
          <h2 class="card-activity">
            <!-- Show delivery -->
            <%= data.delivery %>
          </h2>
          <div class="card-info">
            <span class="card-type">
              <!-- Show Activity type -->
              <%= data.type %>
            </span>
            <span class="card-participants">
              <!-- Show category -->
              <%= data.category %>
              <span class="card-number"></span>
            </span>
          </div>
        </article>
        <%} %> <%if(locals.error){%>
        <div id="tag-error" class="tag-error">
          <!-- Show any errors that were sent from the backend -->
          <%= error %>
        </div>
        <%}%>
      </section>
    </div>
  </body>
</html>
